# Backend dependencies - install from project root: pip install -r requirements.txt
# This file documents backend-specific deps (all in root requirements.txt)
# fastapi, uvicorn, sqlalchemy, psycopg2-binary, python-dotenv
